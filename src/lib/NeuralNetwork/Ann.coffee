class Ann
  constructor: ->

  guess: ->

  # sets the source of the neural network, returning Error for those that arent supported or cannot be connected to.
  _setDataSource: ->

  # sets the neural network data on the source
  _setData: ->

  # gets the neural network data from a source
  _getData: ->

module.exports = Ann
